{
    "name": "management",
    "version": "1.0.0",
    "scripts": {
        "client": "cd cl client && yarn start",
        "serve": "nodemin server.js",
        "dev": "concurrently --kill-other-on-fail \"yarn server\" \"yarn client\""
    },
    "dependencies": {
        "body-parser": "^1.18.3",
        "express": "^4.16.4",
        "nodemon": "^2.0.22"
    },
    "devDependencies": {
        "concurrently": "^4.0.1"
    }
}
